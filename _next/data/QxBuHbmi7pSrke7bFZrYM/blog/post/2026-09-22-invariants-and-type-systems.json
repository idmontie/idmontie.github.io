{"pageProps":{"headTitle":"Invariants and Type Systems - idmontie's Portfolio","headDescription":"My philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make…","headKeywords":"typescript, react","post":{"slug":"2026-09-22-invariants-and-type-systems","date":"2026-09-22","title":"Invariants and Type Systems","frontmatter":{"title":"Invariants and Type Systems","tags":["typescript","react"]},"contentRaw":"\nMy philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function's expectations:\n\n```\nfunction addToCart(itemId, count) {\n  assert(typeof count === 'number', 'Count must be a number');\n  assert(count > 0, 'Count must be greater than 0');\n  assert(count < 10000, 'Count must be less than 10000');\n  assert(typeof itemId === 'string', 'ItemId must be a string');\n  // ...\n}\n```\n\nThe `addToCart` function has guards to check several **preconditions** that must be met before the function can run. This preserves the **invariants** expected by the rest of the system.\n\nWith TypeScript, we can use the type system to encode these preconditions as types:\n\n```\ntype PositiveNumber = Tagged<number, 'PositiveNumber'>;\ntype MaximumCartNumber = Tagged<number, 'MaximumCartNumber'>;\ntype ItemId = Tagged<string, 'ItemId'>;\n```\n\nAnd we can validate via:\n\n```\nfunction positiveNumber(value: number): PositiveNumber {\n  if (!Number.isInteger(value) || value <= 0) {\n    throw new Error('value must be a positive number');\n  }\n\n  return value as PositiveNumber;\n}\n\nfunction maximumCartNumber(value: number): MaximumCartNumber {\n  if (!Number.isInteger(value) || value >= 10000) {\n    throw new Error('value must be less than 10000');\n  }\n\n  return value as MaximumCartNumber;\n}\n```\n\nNow `addToCart` doesn't need to repeat those guards, the validation happens when the domain types are created, and we can allow\nthe type system to enforce the invariants at compile time.\n\n```\nfunction addToCart(itemId: ItemId, count: PositiveNumber & MaximumCartNumber) {\n  // ...\n}\n```\n\n> Make it impossible to create invalid states via the type system.\n\nOnce a value has been validated into a `PositiveNumber`, passing a negative number to `addToCart` is no longer representable without explicitly circumventing the type system.\n\n## React Components\n\nLet’s extend the philosophy to React components. Before TypeScript, React components commonly enforced these constraints in two places: with propTypes and with runtime guards inside the component:\n\n```\nclass CartCount extends Component {\n  static propTypes = {\n    count: PropTypes.number.isRequired;\n  }\n\n  render() {\n    const { count } = this.props;\n\n    if (typeof count !== 'number' || count < 0) {\n      return null;\n    }\n\n    // ...\n  }\n}\n```\n\nThis works for simple components, but as additional logic is added to the component, it becomes less clear where these guards belong\nand whether every code path preserves the invariant.\n\nWith TypeScript, we can use the type system as the guard:\n\n```tsx\nexport function CartCount({ count }: { count: PositiveNumber }) {\n    return <div>{count}</div>;\n}\n```\n\nThe rule here can be best described as:\n\n> Prefer domain types over primitives.\n\nNeed to ensure that the string passed to a function is a URL? Create a `Tagged` type to represent a `URLString`, and add utilities to create the type or assert the type.\n\nWant to enforce that a string passed to a component is translated? Don’t use the raw `string` type, use a `LocalizedString` `Tagged` type that your translation functions return instead. I’ve written about tagged types before, particularly for [localization]((https://idmontie.github.io/blog/post/2023-08-19-localization), because they are an effective way to encode domain constraints.\n\n## Polymorphic Components\n\nDesign-system components often require different sets of props depending on how the component is used. For a concrete example, let’s use a Card component. By default, a card is just a visual container for information:\n\n```\ninterface BaseCardProps {\n  children: JSX.Element;\n}\n```\n\nBut there are times we want it to be clickable:\n\n```\ninterface ClickableCardProps extends BaseCardProps {\n  readonly label: LocalizedString;\n  readonly onClick: () => void;\n  readonly href?: never;\n}\n```\n\nOr we may want it to behave like a link:\n\n```\ninterface LinkCardProps extends BaseCardProps {\n  readonly label: LocalizedString;\n  readonly href: URLString;\n  // Optional onClick, whereas the ClickableCard must ALWAYS have an onClick\n  readonly onClick?: () => void;\n  readonly target?: '_self' | '_blank';\n}\n```\n\nWe can represent these props and still enforce the correct prop usage:\n\n```\ntype CardProps = ClickableCardProps | LinkCardProps;\n```\n\nA Card can never be an ambiguous combination of both a clickable and a link card at the same time.\n\n> Don’t allow ambiguous combinations of component props.\n\nWe encode this information with the type system via a union of mutually exclusive prop shapes that TypeScript can narrow within the component.\n\nHere is a more advanced example that demonstrates how tagged types can be used to enforce invariants.\nWe define a `PositiveNumber` and `EvenNumber` tagged types, and then use them to enforce that a number is both positive and even.\n\n```jsx\nfunction invariant(condition: boolean, message: string): asserts condition {\n  const isProduction = false; // process.env.NODE_ENV === 'production';\n\n  if (!condition) {\n    console.error(message);\n    console.trace();\n\n    // When not in production, we throw an error.\n    if (!isProduction) {\n      throw new Error(message);\n    }\n    return;\n  }\n}\n\ndeclare const positiveBrand: unique symbol;\ndeclare const evenBrand: unique symbol;\ntype PositiveNumber = number & { [positiveBrand]: 'PositiveNumber' };\ntype EvenNumber = number & { [evenBrand]: 'EvenNumber' };\n\nfunction assertPositiveNumber(value: number): asserts value is PositiveNumber {\n  invariant(value > 0, `Value ${value} must be positive`);\n}\n\nfunction assertEvenNumber(value: number): asserts value is EvenNumber {\n    invariant(value % 2 === 0, `Value ${value} must be even`);\n}\n\nfunction assertPositiveEvenNumber(value: number): asserts value is (PositiveNumber & EvenNumber) {\n    assertPositiveNumber(value);\n    assertEvenNumber(value);\n}\n\nfunction multiplyPositiveEvenNumbers(value: PositiveNumber & EvenNumber): PositiveNumber & EvenNumber {\n    // TypeScript cannot infer that multiplication preserves these tagged invariants.\n    // But we know that when we multiply a positive even number by 2, the result is still a positive even number.\n    return value * 2 as (PositiveNumber & EvenNumber);\n}\n\nconst valueTwo = 2;\nassertPositiveEvenNumber(valueTwo);\nconst result = multiplyPositiveEvenNumbers(valueTwo);\nconsole.log(result); // 4\n\n// Throws an error: Value -2 must be positive\nconst valueNegativeTwo = -2;\nassertPositiveEvenNumber(valueNegativeTwo);\nmultiplyPositiveEvenNumbers(valueNegativeTwo);\n\n// Throws an error: Value 5 must be even\nconst valueFive = 5;\nassertPositiveEvenNumber(valueFive);\nmultiplyPositiveEvenNumbers(valueFive);\n\n```\n","contentHTML":"<p>My philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function&#x27;s expectations:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>function addToCart(itemId, count) {\n  assert(typeof count === &#x27;number&#x27;, &#x27;Count must be a number&#x27;);\n  assert(count &gt; 0, &#x27;Count must be greater than 0&#x27;);\n  assert(count &lt; 10000, &#x27;Count must be less than 10000&#x27;);\n  assert(typeof itemId === &#x27;string&#x27;, &#x27;ItemId must be a string&#x27;);\n  // ...\n}\n</code></pre></div>\n<p>The <code>addToCart</code> function has guards to check several <strong>preconditions</strong> that must be met before the function can run. This preserves the <strong>invariants</strong> expected by the rest of the system.</p>\n<p>With TypeScript, we can use the type system to encode these preconditions as types:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>type PositiveNumber = Tagged&lt;number, &#x27;PositiveNumber&#x27;&gt;;\ntype MaximumCartNumber = Tagged&lt;number, &#x27;MaximumCartNumber&#x27;&gt;;\ntype ItemId = Tagged&lt;string, &#x27;ItemId&#x27;&gt;;\n</code></pre></div>\n<p>And we can validate via:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>function positiveNumber(value: number): PositiveNumber {\n  if (!Number.isInteger(value) || value &lt;= 0) {\n    throw new Error(&#x27;value must be a positive number&#x27;);\n  }\n\n  return value as PositiveNumber;\n}\n\nfunction maximumCartNumber(value: number): MaximumCartNumber {\n  if (!Number.isInteger(value) || value &gt;= 10000) {\n    throw new Error(&#x27;value must be less than 10000&#x27;);\n  }\n\n  return value as MaximumCartNumber;\n}\n</code></pre></div>\n<p>Now <code>addToCart</code> doesn&#x27;t need to repeat those guards, the validation happens when the domain types are created, and we can allow\nthe type system to enforce the invariants at compile time.</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>function addToCart(itemId: ItemId, count: PositiveNumber &amp; MaximumCartNumber) {\n  // ...\n}\n</code></pre></div>\n<blockquote class=\"border-l-4 border-gray-300 pl-4\">\n<p>Make it impossible to create invalid states via the type system.</p>\n</blockquote>\n<p>Once a value has been validated into a <code>PositiveNumber</code>, passing a negative number to <code>addToCart</code> is no longer representable without explicitly circumventing the type system.</p>\n<h2>React Components</h2>\n<p>Let’s extend the philosophy to React components. Before TypeScript, React components commonly enforced these constraints in two places: with propTypes and with runtime guards inside the component:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>class CartCount extends Component {\n  static propTypes = {\n    count: PropTypes.number.isRequired;\n  }\n\n  render() {\n    const { count } = this.props;\n\n    if (typeof count !== &#x27;number&#x27; || count &lt; 0) {\n      return null;\n    }\n\n    // ...\n  }\n}\n</code></pre></div>\n<p>This works for simple components, but as additional logic is added to the component, it becomes less clear where these guards belong\nand whether every code path preserves the invariant.</p>\n<p>With TypeScript, we can use the type system as the guard:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">export function CartCount({ count }: { count: PositiveNumber }) {\n    return &lt;div&gt;{count}&lt;/div&gt;;\n}\n</code></pre></div>\n<p>The rule here can be best described as:</p>\n<blockquote class=\"border-l-4 border-gray-300 pl-4\">\n<p>Prefer domain types over primitives.</p>\n</blockquote>\n<p>Need to ensure that the string passed to a function is a URL? Create a <code>Tagged</code> type to represent a <code>URLString</code>, and add utilities to create the type or assert the type.</p>\n<p>Want to enforce that a string passed to a component is translated? Don’t use the raw <code>string</code> type, use a <code>LocalizedString</code> <code>Tagged</code> type that your translation functions return instead. I’ve written about tagged types before, particularly for [localization]((<a href=\"https://idmontie.github.io/blog/post/2023-08-19-localization\">https://idmontie.github.io/blog/post/2023-08-19-localization</a>), because they are an effective way to encode domain constraints.</p>\n<h2>Polymorphic Components</h2>\n<p>Design-system components often require different sets of props depending on how the component is used. For a concrete example, let’s use a Card component. By default, a card is just a visual container for information:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>interface BaseCardProps {\n  children: JSX.Element;\n}\n</code></pre></div>\n<p>But there are times we want it to be clickable:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>interface ClickableCardProps extends BaseCardProps {\n  readonly label: LocalizedString;\n  readonly onClick: () =&gt; void;\n  readonly href?: never;\n}\n</code></pre></div>\n<p>Or we may want it to behave like a link:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>interface LinkCardProps extends BaseCardProps {\n  readonly label: LocalizedString;\n  readonly href: URLString;\n  // Optional onClick, whereas the ClickableCard must ALWAYS have an onClick\n  readonly onClick?: () =&gt; void;\n  readonly target?: &#x27;_self&#x27; | &#x27;_blank&#x27;;\n}\n</code></pre></div>\n<p>We can represent these props and still enforce the correct prop usage:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code>type CardProps = ClickableCardProps | LinkCardProps;\n</code></pre></div>\n<p>A Card can never be an ambiguous combination of both a clickable and a link card at the same time.</p>\n<blockquote class=\"border-l-4 border-gray-300 pl-4\">\n<p>Don’t allow ambiguous combinations of component props.</p>\n</blockquote>\n<p>We encode this information with the type system via a union of mutually exclusive prop shapes that TypeScript can narrow within the component.</p>\n<p>Here is a more advanced example that demonstrates how tagged types can be used to enforce invariants.\nWe define a <code>PositiveNumber</code> and <code>EvenNumber</code> tagged types, and then use them to enforce that a number is both positive and even.</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-jsx\">function invariant(condition: boolean, message: string): asserts condition {\n  const isProduction = false; // process.env.NODE_ENV === &#x27;production&#x27;;\n\n  if (!condition) {\n    console.error(message);\n    console.trace();\n\n    // When not in production, we throw an error.\n    if (!isProduction) {\n      throw new Error(message);\n    }\n    return;\n  }\n}\n\ndeclare const positiveBrand: unique symbol;\ndeclare const evenBrand: unique symbol;\ntype PositiveNumber = number &amp; { [positiveBrand]: &#x27;PositiveNumber&#x27; };\ntype EvenNumber = number &amp; { [evenBrand]: &#x27;EvenNumber&#x27; };\n\nfunction assertPositiveNumber(value: number): asserts value is PositiveNumber {\n  invariant(value &gt; 0, `Value ${value} must be positive`);\n}\n\nfunction assertEvenNumber(value: number): asserts value is EvenNumber {\n    invariant(value % 2 === 0, `Value ${value} must be even`);\n}\n\nfunction assertPositiveEvenNumber(value: number): asserts value is (PositiveNumber &amp; EvenNumber) {\n    assertPositiveNumber(value);\n    assertEvenNumber(value);\n}\n\nfunction multiplyPositiveEvenNumbers(value: PositiveNumber &amp; EvenNumber): PositiveNumber &amp; EvenNumber {\n    // TypeScript cannot infer that multiplication preserves these tagged invariants.\n    // But we know that when we multiply a positive even number by 2, the result is still a positive even number.\n    return value * 2 as (PositiveNumber &amp; EvenNumber);\n}\n\nconst valueTwo = 2;\nassertPositiveEvenNumber(valueTwo);\nconst result = multiplyPositiveEvenNumbers(valueTwo);\nconsole.log(result); // 4\n\n// Throws an error: Value -2 must be positive\nconst valueNegativeTwo = -2;\nassertPositiveEvenNumber(valueNegativeTwo);\nmultiplyPositiveEvenNumbers(valueNegativeTwo);\n\n// Throws an error: Value 5 must be even\nconst valueFive = 5;\nassertPositiveEvenNumber(valueFive);\nmultiplyPositiveEvenNumbers(valueFive);\n\n</code></pre></div>","contentCode":"\"use strict\";\nconst {Fragment: _Fragment, jsx: _jsx, jsxs: _jsxs} = arguments[0];\nfunction _createMdxContent(props) {\n  const _components = {\n    a: \"a\",\n    blockquote: \"blockquote\",\n    code: \"code\",\n    h2: \"h2\",\n    p: \"p\",\n    pre: \"pre\",\n    strong: \"strong\",\n    ...props.components\n  };\n  return _jsxs(_Fragment, {\n    children: [_jsx(_components.p, {\n      children: \"My philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function's expectations:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"function addToCart(itemId, count) {\\n  assert(typeof count === 'number', 'Count must be a number');\\n  assert(count > 0, 'Count must be greater than 0');\\n  assert(count < 10000, 'Count must be less than 10000');\\n  assert(typeof itemId === 'string', 'ItemId must be a string');\\n  // ...\\n}\\n\"\n      })\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"The \", _jsx(_components.code, {\n        children: \"addToCart\"\n      }), \" function has guards to check several \", _jsx(_components.strong, {\n        children: \"preconditions\"\n      }), \" that must be met before the function can run. This preserves the \", _jsx(_components.strong, {\n        children: \"invariants\"\n      }), \" expected by the rest of the system.\"]\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"With TypeScript, we can use the type system to encode these preconditions as types:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"type PositiveNumber = Tagged<number, 'PositiveNumber'>;\\ntype MaximumCartNumber = Tagged<number, 'MaximumCartNumber'>;\\ntype ItemId = Tagged<string, 'ItemId'>;\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"And we can validate via:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"function positiveNumber(value: number): PositiveNumber {\\n  if (!Number.isInteger(value) || value <= 0) {\\n    throw new Error('value must be a positive number');\\n  }\\n\\n  return value as PositiveNumber;\\n}\\n\\nfunction maximumCartNumber(value: number): MaximumCartNumber {\\n  if (!Number.isInteger(value) || value >= 10000) {\\n    throw new Error('value must be less than 10000');\\n  }\\n\\n  return value as MaximumCartNumber;\\n}\\n\"\n      })\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"Now \", _jsx(_components.code, {\n        children: \"addToCart\"\n      }), \" doesn't need to repeat those guards, the validation happens when the domain types are created, and we can allow\\nthe type system to enforce the invariants at compile time.\"]\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"function addToCart(itemId: ItemId, count: PositiveNumber & MaximumCartNumber) {\\n  // ...\\n}\\n\"\n      })\n    }), \"\\n\", _jsxs(_components.blockquote, {\n      children: [\"\\n\", _jsx(_components.p, {\n        children: \"Make it impossible to create invalid states via the type system.\"\n      }), \"\\n\"]\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"Once a value has been validated into a \", _jsx(_components.code, {\n        children: \"PositiveNumber\"\n      }), \", passing a negative number to \", _jsx(_components.code, {\n        children: \"addToCart\"\n      }), \" is no longer representable without explicitly circumventing the type system.\"]\n    }), \"\\n\", _jsx(_components.h2, {\n      children: \"React Components\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Let’s extend the philosophy to React components. Before TypeScript, React components commonly enforced these constraints in two places: with propTypes and with runtime guards inside the component:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"class CartCount extends Component {\\n  static propTypes = {\\n    count: PropTypes.number.isRequired;\\n  }\\n\\n  render() {\\n    const { count } = this.props;\\n\\n    if (typeof count !== 'number' || count < 0) {\\n      return null;\\n    }\\n\\n    // ...\\n  }\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"This works for simple components, but as additional logic is added to the component, it becomes less clear where these guards belong\\nand whether every code path preserves the invariant.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"With TypeScript, we can use the type system as the guard:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"export function CartCount({ count }: { count: PositiveNumber }) {\\n    return <div>{count}</div>;\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"The rule here can be best described as:\"\n    }), \"\\n\", _jsxs(_components.blockquote, {\n      children: [\"\\n\", _jsx(_components.p, {\n        children: \"Prefer domain types over primitives.\"\n      }), \"\\n\"]\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"Need to ensure that the string passed to a function is a URL? Create a \", _jsx(_components.code, {\n        children: \"Tagged\"\n      }), \" type to represent a \", _jsx(_components.code, {\n        children: \"URLString\"\n      }), \", and add utilities to create the type or assert the type.\"]\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"Want to enforce that a string passed to a component is translated? Don’t use the raw \", _jsx(_components.code, {\n        children: \"string\"\n      }), \" type, use a \", _jsx(_components.code, {\n        children: \"LocalizedString\"\n      }), \" \", _jsx(_components.code, {\n        children: \"Tagged\"\n      }), \" type that your translation functions return instead. I’ve written about tagged types before, particularly for [localization]((\", _jsx(_components.a, {\n        href: \"https://idmontie.github.io/blog/post/2023-08-19-localization\",\n        children: \"https://idmontie.github.io/blog/post/2023-08-19-localization\"\n      }), \"), because they are an effective way to encode domain constraints.\"]\n    }), \"\\n\", _jsx(_components.h2, {\n      children: \"Polymorphic Components\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Design-system components often require different sets of props depending on how the component is used. For a concrete example, let’s use a Card component. By default, a card is just a visual container for information:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"interface BaseCardProps {\\n  children: JSX.Element;\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"But there are times we want it to be clickable:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"interface ClickableCardProps extends BaseCardProps {\\n  readonly label: LocalizedString;\\n  readonly onClick: () => void;\\n  readonly href?: never;\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Or we may want it to behave like a link:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"interface LinkCardProps extends BaseCardProps {\\n  readonly label: LocalizedString;\\n  readonly href: URLString;\\n  // Optional onClick, whereas the ClickableCard must ALWAYS have an onClick\\n  readonly onClick?: () => void;\\n  readonly target?: '_self' | '_blank';\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"We can represent these props and still enforce the correct prop usage:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        children: \"type CardProps = ClickableCardProps | LinkCardProps;\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"A Card can never be an ambiguous combination of both a clickable and a link card at the same time.\"\n    }), \"\\n\", _jsxs(_components.blockquote, {\n      children: [\"\\n\", _jsx(_components.p, {\n        children: \"Don’t allow ambiguous combinations of component props.\"\n      }), \"\\n\"]\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"We encode this information with the type system via a union of mutually exclusive prop shapes that TypeScript can narrow within the component.\"\n    }), \"\\n\", _jsxs(_components.p, {\n      children: [\"Here is a more advanced example that demonstrates how tagged types can be used to enforce invariants.\\nWe define a \", _jsx(_components.code, {\n        children: \"PositiveNumber\"\n      }), \" and \", _jsx(_components.code, {\n        children: \"EvenNumber\"\n      }), \" tagged types, and then use them to enforce that a number is both positive and even.\"]\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-jsx\",\n        children: \"function invariant(condition: boolean, message: string): asserts condition {\\n  const isProduction = false; // process.env.NODE_ENV === 'production';\\n\\n  if (!condition) {\\n    console.error(message);\\n    console.trace();\\n\\n    // When not in production, we throw an error.\\n    if (!isProduction) {\\n      throw new Error(message);\\n    }\\n    return;\\n  }\\n}\\n\\ndeclare const positiveBrand: unique symbol;\\ndeclare const evenBrand: unique symbol;\\ntype PositiveNumber = number & { [positiveBrand]: 'PositiveNumber' };\\ntype EvenNumber = number & { [evenBrand]: 'EvenNumber' };\\n\\nfunction assertPositiveNumber(value: number): asserts value is PositiveNumber {\\n  invariant(value > 0, `Value ${value} must be positive`);\\n}\\n\\nfunction assertEvenNumber(value: number): asserts value is EvenNumber {\\n    invariant(value % 2 === 0, `Value ${value} must be even`);\\n}\\n\\nfunction assertPositiveEvenNumber(value: number): asserts value is (PositiveNumber & EvenNumber) {\\n    assertPositiveNumber(value);\\n    assertEvenNumber(value);\\n}\\n\\nfunction multiplyPositiveEvenNumbers(value: PositiveNumber & EvenNumber): PositiveNumber & EvenNumber {\\n    // TypeScript cannot infer that multiplication preserves these tagged invariants.\\n    // But we know that when we multiply a positive even number by 2, the result is still a positive even number.\\n    return value * 2 as (PositiveNumber & EvenNumber);\\n}\\n\\nconst valueTwo = 2;\\nassertPositiveEvenNumber(valueTwo);\\nconst result = multiplyPositiveEvenNumbers(valueTwo);\\nconsole.log(result); // 4\\n\\n// Throws an error: Value -2 must be positive\\nconst valueNegativeTwo = -2;\\nassertPositiveEvenNumber(valueNegativeTwo);\\nmultiplyPositiveEvenNumbers(valueNegativeTwo);\\n\\n// Throws an error: Value 5 must be even\\nconst valueFive = 5;\\nassertPositiveEvenNumber(valueFive);\\nmultiplyPositiveEvenNumbers(valueFive);\\n\\n\"\n      })\n    })]\n  });\n}\nfunction MDXContent(props = {}) {\n  const {wrapper: MDXLayout} = props.components || ({});\n  return MDXLayout ? _jsx(MDXLayout, {\n    ...props,\n    children: _jsx(_createMdxContent, {\n      ...props\n    })\n  }) : _createMdxContent(props);\n}\nreturn {\n  default: MDXContent\n};\n","excerptRaw":"\nMy philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function's expectations:","excerptHTML":"<p>My philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function&#x27;s expectations:</p>","excerptCode":"\"use strict\";\nconst {jsx: _jsx} = arguments[0];\nfunction _createMdxContent(props) {\n  const _components = {\n    p: \"p\",\n    ...props.components\n  };\n  return _jsx(_components.p, {\n    children: \"My philosophy when designing APIs and libraries is to make it as easy to use as possible, but as difficult to use INCORRECTLY as possible. When I used to make plugins with jQuery, there would often be guards on functions to ensure that the data passed in met the function's expectations:\"\n  });\n}\nfunction MDXContent(props = {}) {\n  const {wrapper: MDXLayout} = props.components || ({});\n  return MDXLayout ? _jsx(MDXLayout, {\n    ...props,\n    children: _jsx(_createMdxContent, {\n      ...props\n    })\n  }) : _createMdxContent(props);\n}\nreturn {\n  default: MDXContent\n};\n","tags":["typescript","react"]},"previous":null,"next":{"slug":"2026-09-16-review-tool","title":"Keeping Humans in the Loop for Agent-Written Code"}},"canonicalPath":"/blog/post/2026-09-22-invariants-and-type-systems","__N_SSG":true}