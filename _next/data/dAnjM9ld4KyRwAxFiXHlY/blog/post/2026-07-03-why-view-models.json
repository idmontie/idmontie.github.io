{"pageProps":{"headTitle":"Why view models? - idmontie's Portfolio","headKeywords":"technology, programming","post":{"slug":"2026-07-03-why-view-models","date":"2026-07-03","title":"Why view models?","frontmatter":{"title":"Why view models?","tags":["technology","programming"]},"contentRaw":"\nWhen writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.\n\nFor example, imagine we want to display the account status of a user. The data comes back as\n\n```tsx\ntype UserStatus = 'active' | 'suspended' | 'pending';\n\ninterface User {\n  id: string;\n  // other fields\n  status: UserStatus;\n}\n```\n\nWe add a hook to get the right badge rendering:\n\n```jsx\nfunction useStatusBadge(status: UserStatus): {\n  text: string;\n  variant: 'success' | 'warning' | 'danger';\n} {\n  switch (status) {\n    case 'active':\n      return { text: 'Active', variant: 'success' };\n    case 'suspended':\n      return { text: 'Suspended', variant: 'danger' };\n    case 'pending':\n      return { text: 'Pending', variant: 'warning' };\n   }\n }\n```\n\nThe example is simple, but as the application gets more complicated, the mapping need to include localization, icon choices, or badge variants. That presentation logic can be encapsulated in the single hook.\n\nRather than call this hook in every usage, we could instead transform the data from the API using the View Model pattern. Rather than pass around the User object type, we could instead pass around a modified type:\n\n```tsx\ninterface UserViewModel {\n  id: string;\n  // other fields\n  status: UserStatus;\n  statusText: string;\n  statusVariant: 'success' | 'warning' | 'danger';\n}\n```\n\nWe basically take the raw API response and transform it into a more presentation-ready form.\n\nFor those who have worked on API services before, view models parallel DTOs. A Data Transfer Object maps data between transfer boundaries, while a view model helps represent data in the way we plan to present it.\n\n### Why don’t we see them used more often?\n\nThere are a few reasons we don’t see View Models explicitly called out in React code. For one, any prop interface that describes display values for a React component already acts as a type of View Model:\n\n```tsx\ninterface BadgeProps {\n  text: string;\n  variant: 'success' | 'warning' | 'danger';\n}\n\nfunction Badge(props: BadgeProps): JSX.Element { /* implementation */ }\n```\n\nAnd going further, we may even wrap this component to provide a reusable version for users:\n\n```tsx\ninterface UserBadgeProps {\n  status: UserStatus;\n}\n\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\n  const badgeProps = useStatusBadge(props.status);\n  \n  return <Badge {...badgeProps} />;\n}\n\n```\n\nWith that in mind, this helps explains why we don’t see explicit View Models during typical React development.\n\nOnce you start thinking about the prop interface as a view model is passing too much data into your component:\n\n```tsx\ninterface BadUserBadgeProps {\n  user: User;\n}\n\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\n  const badgeProps = useStatusBadge(props.user.status);\n  \n  return <Badge {...badgeProps} />;\n}\n\n```\n\nIn this example, we pass through the entire User interface into the component. While in the short term this may seem fine, longer term this can create a brittle interface. This starts to matter more when you are server-side rendering, since the props from an Async Server Component to a client component should be as minimal as possible to avoid sending too much data from the server render to the client.\n\n### Sneaky View Models\n\nThere are a few cases where we actually do write View Models without really thinking about it, other than prop interfaces. Whenever we write selectors for accessing data stores that derive data, reshape it, or prepare state for rendering, we are writing some form of View Models. Context selectors are often used to minimize the number of re-renders when the context data changes.\n\nIn Fate, views select on subsets of fields from API data to reduce the impact of data changes causing re-renders.\n\nAll three strategies aim to minimize the impact of data causing re-renders, and in trying to reduce that churn, we end up writing some form of View Model.\n\n### Conclusion\n\nOnce we know the software engineering pattern, we can start to see how it manifests in different areas. We see that while we are not explicitly saying that something is a View Model in React code, we write component interfaces and transform data in ways that prepare it for presentation.","contentHTML":"<p>When writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.</p>\n<p>For example, imagine we want to display the account status of a user. The data comes back as</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">type UserStatus = &#x27;active&#x27; | &#x27;suspended&#x27; | &#x27;pending&#x27;;\n\ninterface User {\n  id: string;\n  // other fields\n  status: UserStatus;\n}\n</code></pre></div>\n<p>We add a hook to get the right badge rendering:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-jsx\">function useStatusBadge(status: UserStatus): {\n  text: string;\n  variant: &#x27;success&#x27; | &#x27;warning&#x27; | &#x27;danger&#x27;;\n} {\n  switch (status) {\n    case &#x27;active&#x27;:\n      return { text: &#x27;Active&#x27;, variant: &#x27;success&#x27; };\n    case &#x27;suspended&#x27;:\n      return { text: &#x27;Suspended&#x27;, variant: &#x27;danger&#x27; };\n    case &#x27;pending&#x27;:\n      return { text: &#x27;Pending&#x27;, variant: &#x27;warning&#x27; };\n   }\n }\n</code></pre></div>\n<p>The example is simple, but as the application gets more complicated, the mapping need to include localization, icon choices, or badge variants. That presentation logic can be encapsulated in the single hook.</p>\n<p>Rather than call this hook in every usage, we could instead transform the data from the API using the View Model pattern. Rather than pass around the User object type, we could instead pass around a modified type:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">interface UserViewModel {\n  id: string;\n  // other fields\n  status: UserStatus;\n  statusText: string;\n  statusVariant: &#x27;success&#x27; | &#x27;warning&#x27; | &#x27;danger&#x27;;\n}\n</code></pre></div>\n<p>We basically take the raw API response and transform it into a more presentation-ready form.</p>\n<p>For those who have worked on API services before, view models parallel DTOs. A Data Transfer Object maps data between transfer boundaries, while a view model helps represent data in the way we plan to present it.</p>\n<h3>Why don’t we see them used more often?</h3>\n<p>There are a few reasons we don’t see View Models explicitly called out in React code. For one, any prop interface that describes display values for a React component already acts as a type of View Model:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">interface BadgeProps {\n  text: string;\n  variant: &#x27;success&#x27; | &#x27;warning&#x27; | &#x27;danger&#x27;;\n}\n\nfunction Badge(props: BadgeProps): JSX.Element { /* implementation */ }\n</code></pre></div>\n<p>And going further, we may even wrap this component to provide a reusable version for users:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">interface UserBadgeProps {\n  status: UserStatus;\n}\n\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\n  const badgeProps = useStatusBadge(props.status);\n  \n  return &lt;Badge {...badgeProps} /&gt;;\n}\n\n</code></pre></div>\n<p>With that in mind, this helps explains why we don’t see explicit View Models during typical React development.</p>\n<p>Once you start thinking about the prop interface as a view model is passing too much data into your component:</p>\n<div class=\"overflow-auto rounded-2xl bg-[#0e005d6d] p-4 font-mono text-sm dark:bg-[#0e005d6d] dark:text-gray-100\"><pre><code class=\"language-tsx\">interface BadUserBadgeProps {\n  user: User;\n}\n\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\n  const badgeProps = useStatusBadge(props.user.status);\n  \n  return &lt;Badge {...badgeProps} /&gt;;\n}\n\n</code></pre></div>\n<p>In this example, we pass through the entire User interface into the component. While in the short term this may seem fine, longer term this can create a brittle interface. This starts to matter more when you are server-side rendering, since the props from an Async Server Component to a client component should be as minimal as possible to avoid sending too much data from the server render to the client.</p>\n<h3>Sneaky View Models</h3>\n<p>There are a few cases where we actually do write View Models without really thinking about it, other than prop interfaces. Whenever we write selectors for accessing data stores that derive data, reshape it, or prepare state for rendering, we are writing some form of View Models. Context selectors are often used to minimize the number of re-renders when the context data changes.</p>\n<p>In Fate, views select on subsets of fields from API data to reduce the impact of data changes causing re-renders.</p>\n<p>All three strategies aim to minimize the impact of data causing re-renders, and in trying to reduce that churn, we end up writing some form of View Model.</p>\n<h3>Conclusion</h3>\n<p>Once we know the software engineering pattern, we can start to see how it manifests in different areas. We see that while we are not explicitly saying that something is a View Model in React code, we write component interfaces and transform data in ways that prepare it for presentation.</p>","contentCode":"\"use strict\";\nconst {Fragment: _Fragment, jsx: _jsx, jsxs: _jsxs} = arguments[0];\nfunction _createMdxContent(props) {\n  const _components = {\n    code: \"code\",\n    h3: \"h3\",\n    p: \"p\",\n    pre: \"pre\",\n    ...props.components\n  };\n  return _jsxs(_Fragment, {\n    children: [_jsx(_components.p, {\n      children: \"When writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"For example, imagine we want to display the account status of a user. The data comes back as\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"type UserStatus = 'active' | 'suspended' | 'pending';\\n\\ninterface User {\\n  id: string;\\n  // other fields\\n  status: UserStatus;\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"We add a hook to get the right badge rendering:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-jsx\",\n        children: \"function useStatusBadge(status: UserStatus): {\\n  text: string;\\n  variant: 'success' | 'warning' | 'danger';\\n} {\\n  switch (status) {\\n    case 'active':\\n      return { text: 'Active', variant: 'success' };\\n    case 'suspended':\\n      return { text: 'Suspended', variant: 'danger' };\\n    case 'pending':\\n      return { text: 'Pending', variant: 'warning' };\\n   }\\n }\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"The example is simple, but as the application gets more complicated, the mapping need to include localization, icon choices, or badge variants. That presentation logic can be encapsulated in the single hook.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Rather than call this hook in every usage, we could instead transform the data from the API using the View Model pattern. Rather than pass around the User object type, we could instead pass around a modified type:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"interface UserViewModel {\\n  id: string;\\n  // other fields\\n  status: UserStatus;\\n  statusText: string;\\n  statusVariant: 'success' | 'warning' | 'danger';\\n}\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"We basically take the raw API response and transform it into a more presentation-ready form.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"For those who have worked on API services before, view models parallel DTOs. A Data Transfer Object maps data between transfer boundaries, while a view model helps represent data in the way we plan to present it.\"\n    }), \"\\n\", _jsx(_components.h3, {\n      children: \"Why don’t we see them used more often?\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"There are a few reasons we don’t see View Models explicitly called out in React code. For one, any prop interface that describes display values for a React component already acts as a type of View Model:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"interface BadgeProps {\\n  text: string;\\n  variant: 'success' | 'warning' | 'danger';\\n}\\n\\nfunction Badge(props: BadgeProps): JSX.Element { /* implementation */ }\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"And going further, we may even wrap this component to provide a reusable version for users:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"interface UserBadgeProps {\\n  status: UserStatus;\\n}\\n\\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\\n  const badgeProps = useStatusBadge(props.status);\\n  \\n  return <Badge {...badgeProps} />;\\n}\\n\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"With that in mind, this helps explains why we don’t see explicit View Models during typical React development.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Once you start thinking about the prop interface as a view model is passing too much data into your component:\"\n    }), \"\\n\", _jsx(_components.pre, {\n      children: _jsx(_components.code, {\n        className: \"language-tsx\",\n        children: \"interface BadUserBadgeProps {\\n  user: User;\\n}\\n\\nfunction UserBadge(props: UserBadgeProps): JSX.Element {\\n  const badgeProps = useStatusBadge(props.user.status);\\n  \\n  return <Badge {...badgeProps} />;\\n}\\n\\n\"\n      })\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"In this example, we pass through the entire User interface into the component. While in the short term this may seem fine, longer term this can create a brittle interface. This starts to matter more when you are server-side rendering, since the props from an Async Server Component to a client component should be as minimal as possible to avoid sending too much data from the server render to the client.\"\n    }), \"\\n\", _jsx(_components.h3, {\n      children: \"Sneaky View Models\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"There are a few cases where we actually do write View Models without really thinking about it, other than prop interfaces. Whenever we write selectors for accessing data stores that derive data, reshape it, or prepare state for rendering, we are writing some form of View Models. Context selectors are often used to minimize the number of re-renders when the context data changes.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"In Fate, views select on subsets of fields from API data to reduce the impact of data changes causing re-renders.\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"All three strategies aim to minimize the impact of data causing re-renders, and in trying to reduce that churn, we end up writing some form of View Model.\"\n    }), \"\\n\", _jsx(_components.h3, {\n      children: \"Conclusion\"\n    }), \"\\n\", _jsx(_components.p, {\n      children: \"Once we know the software engineering pattern, we can start to see how it manifests in different areas. We see that while we are not explicitly saying that something is a View Model in React code, we write component interfaces and transform data in ways that prepare it for presentation.\"\n    })]\n  });\n}\nfunction MDXContent(props = {}) {\n  const {wrapper: MDXLayout} = props.components || ({});\n  return MDXLayout ? _jsx(MDXLayout, {\n    ...props,\n    children: _jsx(_createMdxContent, {\n      ...props\n    })\n  }) : _createMdxContent(props);\n}\nreturn {\n  default: MDXContent\n};\n","excerptRaw":"\nWhen writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.","excerptHTML":"<p>When writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.</p>","excerptCode":"\"use strict\";\nconst {jsx: _jsx} = arguments[0];\nfunction _createMdxContent(props) {\n  const _components = {\n    p: \"p\",\n    ...props.components\n  };\n  return _jsx(_components.p, {\n    children: \"When writing React code, we often need to pull data from an API and manipulate that data to better match how we want to present it. Now that React hooks are ubiquitous, I have seen a lot of that data manipulation move into hooks that get re-used in multiple components.\"\n  });\n}\nfunction MDXContent(props = {}) {\n  const {wrapper: MDXLayout} = props.components || ({});\n  return MDXLayout ? _jsx(MDXLayout, {\n    ...props,\n    children: _jsx(_createMdxContent, {\n      ...props\n    })\n  }) : _createMdxContent(props);\n}\nreturn {\n  default: MDXContent\n};\n","tags":["technology","programming"]},"previous":{"slug":"2026-08-27-toasts","title":"Toasts Are Harder Than They Look"},"next":{"slug":"2026-07-02-what-makes-good-todo","title":"What Makes a Good TODO?"}},"__N_SSG":true}